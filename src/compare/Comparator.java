package compare;

/*
job of the class is to compare between two given lengths.
 */

public class Comparator {
    public boolean compare(Measurement first, Measurement second) {
        return first.compare(second);
    }
}